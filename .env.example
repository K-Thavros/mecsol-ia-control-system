# ===================================================================
# Archivo de Ejemplo de Variables de Entorno para MECSOL AI System
#
# Instrucciones:
# 1. Copia este archivo a .env (cp .env.example .env)
# 2. Rellena los valores de configuración (no secretos).
# 3. Ejecuta 'bash scripts/generate_env_secrets.sh' para generar
#    automáticamente todos los secretos (valores vacíos).
# ===================================================================

# --- Configuración General del Entorno ---
# Zona horaria para los contenedores y aplicaciones.
TZ=America/Monterrey
# Entorno de despliegue (e.g., test, development, production)
ENVIRONMENT=test
# Email para notificaciones de Let's Encrypt sobre los certificados SSL.
EMAIL_ACME=admin@mecsol.mx

# --- Dominios y Subdominios ---
# Asegúrate de que estos dominios apuntan a la IP de tu servidor.
DOMAIN=mecsol-group.com
NOCO_SUBDOMAIN=noco-test
GRAFANA_SUBDOMAIN=grafana-test
N8N_SUBDOMAIN=n8n-test
ODOO_SUBDOMAIN=odoo-test
API_SUBDOMAIN=api-test

# --- Contacto de Negocio (Usado por los agentes de IA) ---
NOTIFY_EMAIL=ventas@mecsol.mx
NOTIFY_WHATSAPP=+528180765721

# ===================================================================
# Secretos - DEJAR EN BLANCO PARA AUTOGENERACIÓN
# El script 'scripts/generate_env_secrets.sh' rellenará estos valores.
# ===================================================================

# --- Base de Datos (PostgreSQL) ---
POSTGRES_USER=odoo
POSTGRES_DB=postgres
POSTGRES_PASSWORD=

# --- Orquestador FastAPI ---
# Secreto para firmar los JSON Web Tokens (JWT) de la aplicación.
JWT_SECRET=

# --- Odoo ---
# Contraseña maestra para la gestión de bases de datos de Odoo.
ODOO_MASTER_PASSWORD=

# --- n8n ---
# Clave de encriptación para las credenciales guardadas en n8n.
N8N_ENCRYPTION_KEY=

# --- Grafana ---
# Contraseña para el usuario admin de Grafana.
GRAFANA_ADMIN_PASSWORD=

# --- APIs de Terceros ---
# Clave de API para el servicio de envío de correos SendGrid.
SENDGRID_API_KEY=
# Auth Token para el servicio de mensajería Twilio (WhatsApp/SMS).
TWILIO_AUTH_TOKEN=
# Clave de API para los modelos de lenguaje de OpenAI.
OPENAI_API_KEY=
